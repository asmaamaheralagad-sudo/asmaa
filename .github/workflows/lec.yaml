name: My Hello Workflow
on:
  push:
    branches:
      -main
jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: print hello
        run: echo "Hello from GitHub Actions!"
      - name: print date
        run: date
      - name: print Name
        run: echo"Asmaa Maher"
